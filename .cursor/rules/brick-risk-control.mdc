---
description: 范围冻结、测试硬停、切片粒度
alwaysApply: true
---

# 风险管控

这是本地控制台 Demo，不要加鉴权、联网、日志平台。

| 信号 | 做 | 别做 |
| --- | --- | --- |
| 以后扩展会痛 | 只做当前技术设计写明的挂钩 | EventBus、游戏对象框架、预留空接口 |
| 改终端探测 / 坐标映射 | 同切片补 `tests/` 用例 | 「以后再补测试」 |
| 顺便做下一款游戏 | 指到路线图对应版本 | 本 diff 里实现青蛙过河等 |

# 硬停

1. **范围**：当前 `TODO.md` 没有的玩法/系统不实现；「范围控制」里的想法默认不写代码。
2. **测试**：改 `ProbeTerminal`、`LogicalToColumns`、`GameRegistry` 必须同切片补测。
3. **切片**：单次改动不超过当前 TODO 一个可单独验收的节。
4. **依赖**：不要引入当前技术设计未点名的第三方库。doctest 已点名，只放 `tests/third_party/`。
