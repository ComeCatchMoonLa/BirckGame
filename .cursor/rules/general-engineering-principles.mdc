---
description: 通用工程原则（适用于本仓库任何改动）
alwaysApply: true
---

# 不要过度设计（YAGNI）

只实现当前小版本 `TODO.md` 实际需要的功能。当前技术设计没有写明的挂钩、空壳、通用框架不要提前搭。

遇到「要不要先做成可扩展的」，默认选「先跑通，再抽象」。

# 规模警示线

明显超出时才拆，不必精确计数：

- 单个源文件不要明显超过 500 行
- 单个函数不要明显超过 300 行
- 函数参数不要明显超过 5 个
- 单个文件夹条目不要明显超过 15 个

# 注释

只写「为什么」（非显而易见的取舍、终端兼容坑），不写「做了什么」的复述。
