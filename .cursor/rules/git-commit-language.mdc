---
description: git commit 与版本 tag 用中文
alwaysApply: true
---

# Commit 与 tag

`git commit` 的标题与正文默认用中文。代码标识符、路径保持英文。用户当次要求英文则按当次要求。

## Tag

关键节点打**附注 tag**，版本号与 [`Docs/开发计划/README.md`](Docs/开发计划/README.md) 两段式 `主.次` 一致，前缀 `v`，**无补丁段**（不要 `v1.3.0`）。

| 种类 | 例子 | 何时打 |
| --- | --- | --- |
| 小版本 | `v0.1`、`v1.3` | 该号可验收：0.x 看 TODO/验收；1.x 看该号能进菜单且需求对上 |
| 大里程碑 | 同为 `v主.次`（`v0.5` 引擎收口、`v1.0` 88 款都能进） | 计划里的阶段门槛，不是另做第三段号 |

- 说明用中文，写清边界（引擎能力或玩法编号）。
- 一次提交落地多个小版本：每个号各打一个 tag，可指向同一 commit。
- 未完成切片、日常修复不要打版本 tag。
- `1.0` 是全清单门槛，不要插在 `1.1` 前面。
- 用户要求推送时，版本 tag 与 commit 一起 `git push origin`（含 `--tags` 或推指定 tag）。
