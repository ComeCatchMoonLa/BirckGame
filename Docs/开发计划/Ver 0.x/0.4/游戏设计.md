# 0.4 游戏设计：退出回菜单

> **交付目标：游戏里按 Esc 结束进程，启动器菜单重新出现。空格暂停保持。**

## 做 / 不做

| 做 | 不做 |
| --- | --- |
| Esc（键码 27）调用 `QuitToLauncher` | 把 `run()` 收成可返回函数 |
| `Pause` 等待空格时 Esc 同样退出 | 改碰撞 / 计分 |

## 验收

- 从启动器进俄罗斯方块或迷宫，Esc 后回到菜单。
- 空格仍暂停；Game Over 空格仍重开。
